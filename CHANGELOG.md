# 变更记录

## 0.2.18 - 2026-08-01

- 修复多图生成过程中第一张后的短暂停顿被误判为整组结束；以当前助手回复的完成动作和唯一图片组联合判断。
- 扩展停止/流式状态识别，兼容新的停止按钮与 streaming 标记。
- 无法取得完整回复证据且图片少于安全线时标记为检测不确定，不再误记为额度触顶或跳过素材。

## 0.2.17 - 2026-08-01

- 自动上传前同时检查当前 GPT 输入框中的未发送附件和未发送文字；任一残留都会用结构化错误暂停整批，避免刷新后再次粘贴同一提示词。
- 每个素材任务只能上传其帖子文件夹内部的文件，混入其他帖子时直接拒绝。
- 修复无提示词模式变量作用域错误；附件计数只检查当前 composer，不再扫描历史聊天消息。
- 真实 Plus 桌面验证完成计划解析、单次发送 `1` 及低图片数停止路径；下一帖未被叠加上传。

## 0.2.16 - 2026-08-01

- 自动下载与手动下载统一回写当前助手图片回复的下载状态。
- 新增持久的 `已下载 x/x` / `已打包 x/x` 标记，页面刷新和延迟挂载按钮后仍可恢复。
- 标记继续只绑定助手生成图片，不会把用户上传素材误认为已下载作品。

## 0.2.14 - 2026-08-01

- 网页执行状态机真实跑通两套完整 Plus 作品：计划、单次 `1`、本轮图片、文案、TXT、查重打包和归档均由同一批次绑定。
- 用户上传消息不再显示下载控件；生成回复只保留一组“下载本组/下载并打包”，文案回复保留复制与 TXT 下载。
- 图片不足直接返回不完整并跳过，不发送续图或补图提示，不误打包。
- 提示词库与下载控件改为独立高层浮层，修复点击无响应、重复注入和延迟出现。
- 自动生产核心拆为 `gpt-automation-core.js`；14/14 架构测试与语法检查通过。

## 0.2.12 - 2026-07-31

- 自动生产任务支持独立额度组、预期图片数、最低图片数、可配置自动确认文字和小红书文案请求词。
- 生图完成后可一边下载本轮图片、一边发送文案请求，缩短单窗口等待时间。
- 图片不足时明确标记为生成不完整，不自动写成完整作品，也不触发打包和素材归档。
- GPT 回复下的“下载本组”“下载并打包”严格绑定当前回复，下载文件使用同一批次号并核对实际数量。
- 网页内提示词库、下载按钮和打包按钮可由工作台设置开关控制，按钮使用独立高层级，避免落到网页底部或被原生内容遮挡。
- 工作包请求携带真实下载暂存目录、成品库目录、批次号和预期图片数。
- 兼容 Electron 内嵌环境缺少 `chrome.downloads` 与 `chrome.action` 的情况；Chrome 扩展环境仍保留原生下载能力。

## 0.2.11 - 2026-07-30

- 内置工作台任务改用 ChatGPT 原生附件预览数量和新增用户消息数确认真实上传、真实发送；发送失败最多在当前动作内重试三次，不重复创建作品任务。
- 模板初始化允许简短确认；素材计划必须满足页码与迁移结构要求，过短或不合格时会自动发送一次纠正要求，再决定继续或暂停。
- 下载阶段支持断点续跑。刷新或重试时不再重新上传素材、不重复发送 `1`，而是从本轮已经生成的图片继续保存和打包。
- 生成图片改为带当前 ChatGPT 登录凭据读取后，通过工作台受限接口直接保存。工作台会校验文件名、MIME、体积、尺寸和实际图片格式，避免浏览器下载事件或 Cookie 隔离造成假完成。
- 请求小红书文案后若正文短于 80 字，自动要求补齐一份不少于 300 字的完整文案；第二次仍不完整就安全暂停，不写 TXT、不显示打包成功。
- 已在真实已登录 ChatGPT Plus 会话验证模板初始化、素材上传、计划纠正、自动确认、真实图片生成和 3:4 PNG 落盘。官网本次连续返回截断文案，因此全自动 TXT 与打包仍按安全规则暂停；本地尾段由工作台另行验证。

## 0.2.5 - 2026-07-30

- 工作台任务新增模板初始化和素材生产两类角色；同一模板只初始化一个新会话，再串行处理该模板下的全部素材。
- 自动流程写入计划、图片、文案和打包检查点。失败重试时从最近完成阶段继续，不重复上传附件、发送 `1` 或下载已经验证的文件。
- 每个阶段持续向工作台回传状态、百分比、错误原因和最终路径；上传与实际生成数量同时写入账号滚动额度账本。
- 消息区的“下载本组”“下载并打包”“复制文案/下载 TXT”继续绑定当前回复和当前批次，与自动模式复用同一底层下载能力。
- 只有工作台确认图片、TXT、批次与成品目录完整后才回传完整成功；归档失败会单独标记，不把已经完成的作品重新生成。

## 0.2.4 - 2026-07-30

- 内置模式新增完整自动生产闭环：上传母版与当前素材、等待迁移计划、按设置发送 `1`、等待新生成图片、请求小红书文案、复制正文、下载图片并打包。
- 下载文件统一带一次性批次编号；本地工作台只接收这一批明确命名的图片，校验张数和 TXT 后才返回完成，避免把下载目录中的旧图混进新作品。
- 自动流程会识别登录、平台验证、限流、附件失败、生成超时和文案为空等状态并暂停，不用假进度掩盖失败。
- 生产间隔使用用户设置的随机范围；它只负责队列节奏，不绕过平台验证或限流。
- 自动化结果细分为计划完成、图片已下载、文案已复制、完整工作包已落盘，未完成的阶段不再显示为全流程成功。

## 0.2.3 - 2026-07-30

- 新增团建工作台内置模式：保留真实 ChatGPT 页面和原生附件入口，隐藏重复的扩展生产舱。
- 支持从工作台接收“一个母版 + 多个素材”的逐组任务，使用自定义提示词并把成功、失败、取消结果回传工作台。
- 本机接口地址可随 4327 正式端、4337 开发端或隔离测试端动态切换；允许范围仍限定为 `127.0.0.1`。
- 嵌入态本地文件请求可直接访问当前工作台，避免服务工作线程桥接超时；登录后会尝试唤起 GPT 原生“+ / 附件”入口。
- 自动化测试 9/9 通过；真实隔离桌面烟测已覆盖扩展加载、任务接收和未登录失败反馈，登录后的原生附件成功仍需首次人工登录后验收。

## 0.2.2 - 2026-07-28

- 保留“团建游戏、团建转化、合集攻略”三个母标签，增加可展开的“季节”和“节日”筛选组。
- 季节组统一春天/春日、夏天/夏日等同义标签；节日组统一五一/劳动节、六一/儿童节等同义标签。
- 地区不作为右侧独立筛选维度，地区素材继续由“团建转化”母标签承接。
- 母标签、季节、节日、使用次数和关键词可以组合筛选；无素材的季节或节日子标签不占用界面空间。

## 0.2.1 - 2026-07-27

- 素材筛选改为读取工作台生成的全库索引，不再只筛选已经展开的分类。
- 母标签和使用次数旁显示真实全库数量；全库索引在后台逐分类建立并显示进度，不阻塞工作台启动。
- 使用次数自动结合旧素材链接记录、制作日志和扩展实时台账；只有唯一匹配的历史证据才自动计数，不确定记录进入待核对。
- 筛选结果显示使用数据来源；大结果集限制为前 240 条并提示继续搜索，避免 ChatGPT 页面卡顿。
- 从全库筛选结果点击“传 GPT”时自动懒加载所属分类和真实附件。

## 0.2.0 - 2026-07-27

- 配合工作台 0.9.0 使用真正的分类懒加载：首次连接只取轻量分类索引，展开时才读取该分类，并在定时刷新时保留已加载内容。
- 增加提示词入口独立事件回归测试，确保关闭左侧会话树后“提示词”按钮和面板仍可点击。
- 素材文件夹新增独有身份哈希；同卷改名或移动后仍绑定同一份人工母标签和使用次数，内容相同的两个文件夹也不会共用标签。
- 每个素材文件夹只显示一个母标签：`团建游戏`、`团建转化` 或 `合集攻略`；聚会游戏、破冰游戏等同义分类统一归为“团建游戏”。
- 素材区新增母标签、使用次数、名称/标签/哈希组合筛选。
- 每行新增可配置快捷按钮，可直接改母标签、把使用次数加一，或按设置移动到固定目录；按钮名称、显示与固定目录保存在扩展本地设置中。
- 人工母标签优先于自动识别；自动识别结果不会覆盖人工选择。

## 0.1.9 - 2026-07-27

- 停止接管和搬动 ChatGPT 原生左侧会话节点，避免原生会话点击卡住；扩展继续保留提示词、下载、打包和右侧生产舱。
- 修复关闭会话树后提示词面板按钮失效的问题；提示词新增、编辑、插入、同步、回退和关闭改为面板自身独立处理。
- 上传只使用 ChatGPT 的原生 `#upload-files` 附件入口，并以原生附件预览真正出现作为成功条件，不再误用页面上的其他文件输入框。
- 上传生产指令时保留输入框里已有文案和附件，在原文后追加指令，不再清空草稿。
- 拖放范围收紧到 ChatGPT 中间主区域，不再拦截左侧原生会话和导航操作。
- 成品/素材文件夹可拖到同一授权库内的目标文件夹；执行前显示来源和目标并二次确认，由本地工作台完成真实移动。

## 0.1.8 - 2026-07-26

- 上传前新增素材内容指纹校验，不再只依赖文件夹路径。
- 已使用素材即使被改名或移动，只要图片和文案内容相同，仍会被自动拦截。
- 重复拦截会明确提示是路径命中还是内容指纹命中。

## 0.1.7 - 2026-07-26

- 素材进入 ChatGPT 后自动记录为“已加入 GPT”；真正发送当前消息后自动记录为“已使用”。
- 已使用素材在侧栏变灰并禁用再次上传，防止同一帖子重复生产。
- 使用记录写入本地工作台台账，不移动或改名原始素材文件夹。

## 0.1.6 - 2026-07-26

- “打包作品”改为由浏览器拓展调用本地工作台接口，再由工作台直接执行现有 PowerShell 工作包脚本；默认流程不再唤起 VBS 或自定义协议。
- 成品区改为读取完整本地目录树，不再复用手机分发筛选结果；“已发送”“发布空间”、新下载打包目录和普通文件都会显示。
- 成品区支持逐级展开文件夹，并可把文件夹内可上传的图片、TXT、Markdown 文件传入当前 ChatGPT 对话。
- 增加真实本地接口与回归测试，确认新版链路返回 `workbench-direct` 且 `fallback=false`。

## 0.1.5 - 2026-07-26

- 完成真实 Microsoft Edge + 已登录 ChatGPT 全链路验收：本地帖子 1 张图和 4 个文档进入官网原生附件区，消息进入真实会话并获得生产计划。
- 在同一真实会话确认生产后生成 5 张不同的 ChatGPT 成品图；网页下载控件完成 `5/5`，5 张 PNG 真实写入 `D:\Download`。
- 每张下载均写入工作台本地事件台账，记录真实落盘路径、来源会话、字节数和完成时间。
- 与 ChatGPT 原生视觉进一步收拢：去除突兀的英文营销标题、绿色渐变和重阴影，改为紧凑的灰白工具侧栏。
- 明确验收口径：模拟页面和组件测试只算开发检查；只有真实会话、真实附件、真实下载和本地台账同时成立，才能标记生产链路通过。

## 0.1.4 - 2026-07-26

- 修复 ChatGPT HTTPS 页面直接访问本地端口可能被浏览器私有网络策略拦截的问题：目录、文件和去重请求统一改由扩展后台安全桥接，本地素材树可以稳定读取。
- “传 GPT”改为顺序上传队列，显示等待、历史检查、文件读取、放入 GPT、成功、重复、失败和取消状态；支持取消与失败重试。
- 拖拽素材时增加覆盖 ChatGPT 中间区的明确投放提示，松开后与点击“传 GPT”共用同一条真实上传链路。
- 上传前读取 TXT/Markdown 并查询旧工作包历史；命中历史记录时直接拦截，不把重复作品重新放入 GPT。
- 增加本地目录、GPT 附件入口和历史去重库三项自动自检指示。
- 修正扩展版 `GM_download`：只有浏览器确认文件真正下载完成后才回调成功，不再把“开始下载”误判成“下载完成”；同步提供进度、失败和取消回调。
- 下载完成事件写入本地工作台运行数据，保留来源会话与真实落盘路径，继续兼容既有打包协议和历史数据库。
- 修复素材分类懒加载后界面重绘导致文件夹立即合上的问题；展开状态现在会保留，帖子子文件夹稳定可见。
- 修复每 60 秒后台刷新会清空已展开文件树的问题；刷新现在保留已加载子项，仅在根目录真正变化时重置。
- 素材子项改为真实文件夹节点，显示图片与文档数量，整行支持拖入 ChatGPT。
- 拖放监听改为优先捕获，避免被 ChatGPT 网页自身的拖拽处理截断。
- 上传入口优先锁定 ChatGPT 的 `#upload-files`，排除会话树导入文件框，防止文件送错入口。
- 增加附件数量校验、上传中状态和附件预览确认；成功后再显示完成提示。
- 生产舱字体继承 ChatGPT 网页字体，并统一到网页正文尺度。

## 0.1.3 - 2026-07-26

- 内置现有团建素材库与成品库默认地址，扩展首次打开即可显示，不再要求手工填写。
- 默认地址写入扩展本地数据；工作台连接后自动用工作台当前有效路径同步更新，仍支持以后换目录。
- 即使工作台暂未启动，成品区与素材区的地址栏也会先显示已内置路径，便于判断连接状态。
- 修复工作台晚于浏览器启动时插件停留在连接失败状态的问题；断线后每 5 秒自动重连，连接成功后定期刷新。
- 粘贴成品或素材路径后自动提交并扫描，不再要求额外点击“读取”或按回车。
- 成品区改为文件管理器式树状结构：作品集作为文件夹展开，具体作品缩进显示并保留“传 GPT”；不再把所有作品另列在合集列表底部。

## 0.1.2 - 2026-07-26

- 修复 ChatGPT 初始化完成后清理直接挂在 `<html>` 下的扩展节点，导致生产舱闪现后整体消失的问题。
- 生产舱和独立展开按钮改为挂载到页面正文，并加入节点丢失自动重新挂载守护，兼容 ChatGPT 单页导航和页面重建。

## 0.1.1 - 2026-07-26

- 修复生产舱在新开 ChatGPT 页面时读取旧折叠状态、闪现后消失的问题；每次打开页面现在默认展开。
- 将重新展开入口从会一起移出屏幕的面板内部拆成独立固定按钮；折叠后右侧始终保留“创作舱”入口。
- 增加默认展开与独立折叠按钮的自动化回归断言。

## 0.1.0 - 2026-07-26

- 建立独立浏览器扩展项目，不再作为工作台 EXE 的内置目录。
- 内置最新版 ChatGPT 会话树模块。
- 新增右侧上下分区生产舱：成品区、素材区、目录粘贴设置。
- 支持点击或拖动，把本地图片与文案放入当前 ChatGPT 对话。
- 显示既有作品历史库的精确哈希与视觉哈希覆盖状态。
