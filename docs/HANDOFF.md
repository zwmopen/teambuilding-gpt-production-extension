# 交接

- 当前版本：0.1.8
- 源码真源：`src/`
- 依赖的本机接口：`http://127.0.0.1:4327`
- 本机接口提供方：`teambuilding-workflow-dashboard`
- 内置会话树来源：`chatgpt-conversation-tree` 1.12.4，通过工作台同步脚本机械更新
- 本地打包器来源：`chatgpt-conversation-tree/src/work-package` 1.5.0
- 生产历史库：由本机接口只读汇总，不进入扩展仓库

## 0.1.8 当前事实

- 上传前调用 `/api/extension/material-usage-check`，按路径和聚合 SHA-256 内容指纹双重检查。
- 素材改名或移动后仍可通过内容指纹命中历史 `used` 记录。

## 0.1.7 当前事实

- 素材真实进入 ChatGPT 附件区后记录为 `prepared`，页面真正发送后记录为 `used`。
- `used` 素材在侧栏显示“已使用”并禁止再次上传；原素材文件夹不移动、不改名。
- 使用台账由本地工作台持久化到运行数据目录，拓展自身不直接写磁盘。

## 0.1.6 当前事实

- 默认打包链路：ChatGPT 页面拓展按钮 → 拓展后台 → 本地工作台 API → `make_work_package.ps1`。
- 默认链路不再调用 VBS 或 `cgpt-workpkg://`；VBS 仅保留为旧流程兼容资产。
- 成品区已改用完整本地文件树，和手机分发筛选解耦。
- 已用真实成品目录验证顶层返回 28 个条目，包含“已发送”“发布空间”和近期打包目录。
- 已用预演模式验证工作台返回 `mode=workbench-direct`、`fallback=false`，未移动用户文件。
- Edge 扩展管理页应重新加载 `src`，并确认显示版本 `0.1.8`；旧版本不包含最新修复。

## 已完成

- 独立扩展项目边界。
- 左侧会话树、中间真实 ChatGPT、右侧成品/素材双区布局。
- 成品库和素材库路径粘贴设置。
- 分类懒加载、作品展开、点击上传和拖动上传。
- 生产历史库 193 组图片哈希状态读取。
- 扩展产品名称、版本和更新边界已与团建内容工作台完全分离。
- 已声明会话树在线更新源所需的只读网络权限。
- 修复旧折叠状态导致新页面闪现后消失；新页面默认展开，折叠后由面板外独立固定按钮重新打开。
- 真实页面探针确认 ChatGPT 初始化后会删除直接挂在 `<html>` 下的两个扩展节点；0.1.2 改挂到 `body` 并增加节点丢失自动重新挂载。
- 内置当前团建成品库与素材库默认地址，并写入扩展本地路径数据；工作台连接成功后以工作台有效配置覆盖默认值。
- 本地工作台断线时每 5 秒自动重连，连接后每 60 秒刷新；路径粘贴会自动保存并触发目录扫描。
- 成品区与素材区均为文件管理树；作品集/素材分类是父文件夹，具体作品/帖子为可上传子项。
- 修复素材懒加载重绘后父文件夹自动合上的状态丢失；素材子文件夹显示图片/文档数量并可整行拖拽。
- 后台定时刷新改为合并新数据并保留已加载/已展开节点，只有根目录变化才重置文件树。
- 上传固定使用 ChatGPT 的真实附件框 `#upload-files`，加入文件数校验、上传中状态和附件预览确认。
- 生产舱继承 ChatGPT 的系统字体，正文基准字号为 14px。

## 验收

- 静态架构测试和 JavaScript 语法检查通过。
- 本机接口实测：21 个作品集、11 个素材分类、193 组生产历史。
- 真实 ChatGPT 页面已确认：0.1.3 能读取 21 个作品集、11 个素材分类、193 组历史，路径与网页系统字体正确。
- 真实 Edge + 已登录 ChatGPT 验收通过：1 张本地图片与 4 个本地文档进入官网原生附件区，生产指令提交到真实会话。
- 真实会话按计划生成 5 张不同的成品图，网页下载进度完成 `5/5`，5 张 PNG 实际落盘并写入本地下载事件台账。
- 模拟框、静态 fixture 和组件测试不得再写成“真实上传成功”；它们只用于开发回归。
- 内置会话树模块 SHA-256：`267CEBF793082417C5A10822F391479D3659A0008B722C11E2249BAD48D1A991`

## 默认协作与沉淀规则

- 用户在对话中补充的流程、偏好、验收标准和边界默认属于项目资产；实现后同步更新代码测试、README、变更记录和本交接文档，不要求用户重复提醒。
- 新要求与旧要求冲突时，以较新的明确要求为准；无冲突时全部累积。
- 对外部系统的“成功”必须有真实外部效果证据：真实 ChatGPT 会话、原生附件、真实生成结果、真实落盘文件或真实设备接收状态。
- 任何只在模拟页、测试夹或本地假数据中成立的结果，必须明确标注为组件测试，不能代替最终验收。

## 0.1.5 真实全链路验收

- 会话：`https://chatgpt.com/c/6a65f78b-fc74-83e8-9ffa-8f19374ec780`
- 原生附件：`rough_cut_preview_contact_sheet.jpg`、`配镜说明.md`、`文案_带时间戳.txt`、`文案.txt`、`质检报告.md`。
- 生产：ChatGPT 读取素材并生成 5 页计划；回复 `1` 后生成 5 个不同的官网图片文件。
- 下载：页面控件从 `0/5` 到 `下载完成 5/5`；`D:\Download` 真实新增 5 张 PNG。
- 台账：`D:\AICode\运行数据\江湖有旅人\图文生产控制台\防重复账本\extension-download-events.json` 新增 5 条完成记录。

## 0.1.4 候选验收（已被 0.1.5 真实验收取代）

- 使用独立 Edge 测试配置真实加载 `src`，扩展后台、内容脚本和本地工作台均为实际运行代码，不是静态页面模拟。
- 自动读取结果：21 个作品集、11 个素材分类、193 组生产历史；本地目录、GPT 上传入口、历史去重三项自检全部正常。
- 点击“传 GPT”实测：读取 1 个真实素材帖子，5 个附件成功进入 `#upload-files`，生产指令成功写入 GPT 输入框。
- 拖拽实测：真实素材文件夹拖入 GPT 中间区后，7 个附件成功进入附件框，队列状态变为“已进入附件区”。
- 重复拦截实测：从 `.作品集_041[转]` 选择已打包作品，上传前命中历史标题并标记“已拦截重复”，未再次进入附件框。
- 下载回调实测：扩展隔离环境调用 `GM_download`，只有文件真实完成后才收到 `complete`，不再提前推进打包流程。
- 扩展 JavaScript 语法检查与架构测试通过；工作台 35 项测试通过。
- 候选发布包：`releases/0.1.4/团建GPT数字作品生产助手-0.1.4.zip`
- 候选发布包 SHA-256：`E67D96FA7685861D70A1B2023F12FCECD3F7A58FA70273BE76C1EA2733D98168`

## 更新提示

- 当前 Edge 已加载的 0.1.4 完成了真实生产验收；0.1.5 含视觉与文档收口，发布后在扩展管理页重新加载一次即可生效。
